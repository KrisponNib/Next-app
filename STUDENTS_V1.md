# Students V1

נוסף ל-NEXT לופ ראשון לפלטפורמת תלמידי תופים.

## מה עובד בגרסה הזאת

- לשונית "תלמידים" בתפריט הראשי.
- יצירת תלמיד עם מטרה ומסלול.
- פרופיל אימון מינימלי: משך אימון רגיל + הגדרת הצלחה.
- סגירת שיעור: מה עבדנו, מה הלך טוב, מה לשפר, משימה לשבוע.
- "מה לתרגל עכשיו?" שמייצר אימון לפי:
  - המטרה הנוכחית
  - משימה פתוחה מהשיעור
  - Reflection קודם
  - הזמן הזמין
- הסבר "למה זה האימון".
- Reflection בסוף האימון.
- סימון המשימה כהושלמה / נתקעה לפי ה-Reflection.
- Wins לתלמיד.
- כל הנתונים נכנסים למנגנון ה-state וה-backup הקיים של NEXT.

## מגבלה חשובה של V1

הפרויקט הנוכחי עדיין שומר state ב-localStorage. לכן Student Preview הוא כרגע בדיקה של החוויה באותו דפדפן, ולא פורטל שתלמיד יכול לפתוח ממכשיר אחר.

כדי להפוך את זה לפלטפורמה אמיתית לתלמידים, השלב הבא הוא להעביר את student data ל-Supabase עם authentication / access מתאים. לא כדאי ליצור מערכת state נפרדת לפני המעבר הזה.

## בדיקה מקומית

```bash
npm install
npm run dev
```

ואז לפתוח:

- `/students`
- ליצור תלמיד
- להגדיר דקות רגילות ו"איך נראית הצלחה"
- לסגור שיעור עם משימה
- לפתוח "מה לתרגל עכשיו?"
- לסיים אימון ולשמור Reflection

## Build note

בסביבת העריכה שבה הקוד הוכן, התקנת npm dependencies לא הסתיימה בזמן ולכן לא בוצע `next build` מלא. מומלץ להריץ `npm install && npm run build` מקומית או להסתמך על build preview ב-Netlify לפני merge לפרודקשן.
