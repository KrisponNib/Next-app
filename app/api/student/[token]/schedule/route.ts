import { NextRequest, NextResponse } from "next/server";
import { getLessonSchedule,getStudentByToken,saveLessonSchedule } from "@/lib/cloud/supabaseRest";
import { addMinutes,bookingConflicts,upcomingBookableDays } from "@/lib/scheduling";
export async function GET(_req:NextRequest,{params}:{params:{token:string}}){try{const student=await getStudentByToken(params.token);if(!student)return NextResponse.json({error:"not_found"},{status:404});const schedule=await getLessonSchedule();const today=new Date().toISOString().slice(0,10);const existing=schedule.bookings.filter(b=>b.studentId===student.id&&b.status==="booked"&&b.date>=today).sort((a,b)=>`${a.date}${a.startTime}`.localeCompare(`${b.date}${b.startTime}`))[0];return NextResponse.json({days:upcomingBookableDays(schedule),existing:existing||null})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"unknown error"},{status:500})}}
export async function POST(req:NextRequest,{params}:{params:{token:string}}){try{const student=await getStudentByToken(params.token);if(!student)return NextResponse.json({error:"not_found"},{status:404});const body=await req.json();const schedule=await getLessonSchedule();if(body.action==="request"){const message=String(body.message||"").trim();if(!message)return NextResponse.json({error:"missing_message"},{status:400});schedule.requests.unshift({id:crypto.randomUUID(),studentId:student.id,studentName:student.name,preferredDay:body.preferredDay?String(body.preferredDay):undefined,message,status:"pending",createdAt:new Date().toISOString()});await saveLessonSchedule(schedule);return NextResponse.json({ok:true,kind:"request"})}if(body.action==="book"){const date=String(body.date||""),startTime=String(body.startTime||"");const offered=upcomingBookableDays(schedule).find(d=>d.date===date)?.times||[];if(!offered.includes(startTime))return NextResponse.json({error:"slot_not_offered"},{status:409});if(bookingConflicts(schedule.bookings,date,startTime,schedule.lessonMinutes))return NextResponse.json({error:"slot_taken"},{status:409});schedule.bookings.push({id:crypto.randomUUID(),studentId:student.id,studentName:student.name,date,startTime,endTime:addMinutes(startTime,schedule.lessonMinutes),status:"booked",createdAt:new Date().toISOString()});await saveLessonSchedule(schedule);return NextResponse.json({ok:true,kind:"booking"})}return NextResponse.json({error:"unknown_action"},{status:400})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"unknown error"},{status:500})}}
