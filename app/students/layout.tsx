import { StudentsCloudBridge } from "./StudentsCloudBridge";

export default function StudentsLayout({ children }: { children: React.ReactNode }) {
  return <StudentsCloudBridge>{children}</StudentsCloudBridge>;
}
